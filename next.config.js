/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "cdn1.ozone.ru",
            "images.pexels.com"
        ]
    }
}

module.exports = nextConfig
